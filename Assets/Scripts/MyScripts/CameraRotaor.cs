using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraRotaor : MonoBehaviour
{
    
   /* public string aninm1;
    public PlayerMovement playerMovement;

    Animation animation;

    // Start is called before the first frame update
    void Start()
    {
        animation = GetComponent<Animation>();
    }

    // Update is called once per frame
    void Update()
    {
        if (playerMovement.currentMoveSpeed > 0.0f)
        {
            if (!animation.IsPlaying(aninm1))
                animation.Play(aninm1);
        } else {
            animation.Stop();
        }
        
    }*/
}
